# Supabase — from your project's API settings
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=      # server-only. Used by the Redash sync route to bypass RLS.

# Redash — server-only, never exposed to the browser
REDASH_BASE_URL=https://redash.stayvista.com   # placeholder — confirm real host
REDASH_API_KEY=
