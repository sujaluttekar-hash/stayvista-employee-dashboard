# Preview mode — Supabase removed for now (re-add NEXT_PUBLIC_SUPABASE_URL,
# NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY when it returns).

# Optional: only needed to test the Data role's Redash sync
REDASH_BASE_URL=
REDASH_API_KEY=
